server {
    listen 3000;

    location /api {
        proxy_pass http://app:8080;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
